<div class="container">
  <div class="row">
    <div class="mx-auto col-11 col-sm-10 col-lg-8 col-xl-6">

      <h1>welcome to <i class="fa-solid fa-book"></i><strong>Bookers</strong> !!</h1>
      <p>In <i class="fa-solid fa-book"></i><strong>Bookers</strong>,</p>
      <p>you can share and exchange your opinions , inpressions , and emotions</p>
      <p>about various books!</p>

      <div class="col-10 mx-auto">
        <div class="row mb-3">
          <% if Current.user %>
            <%= link_to "Log in", user_path(Current.user), class: "btn btn-info btn-sm w-100" %>
          <% else %>
            <%= link_to "Log in", new_session_path, class: "btn btn-info btn-sm w-100" %>
          <% end %>
        </div>
        <div class="row">
          <% if Current.user %>
            <%= link_to "Sign up", user_path(Current.user), class: "btn btn-success btn-sm w-100" %>
          <% else %>
            <%= link_to "Sign up", new_user_path, class: "btn btn-success btn-sm w-100" %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>