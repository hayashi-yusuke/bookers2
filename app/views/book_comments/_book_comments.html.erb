<p>コメント数: <%= book.book_comments.count %></p>
<% book.book_comments.each do |book_comment| %>
  <p><%= book_comment.user.name %></p>
  <p><%= book_comment.created_at.strftime('%Y/%m/%d')%></p>
  <p><%= book_comment.comment %></p>
  <% if book_comment.user == Current.user %>
    <%= link_to "削除", book_book_comment_path(book,book_comment), data: { turbo_method: :delete }, class: "btn btn-danger btn-sm" %>
  <% end %>
<% end %>