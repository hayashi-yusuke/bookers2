<% if book.favorited_by?(Current.user) %>
  <%= button_to "♥ #{book.favorites.count}", book_favorite_path(book), method: :delete %>
<% else %>
  <%= button_to "♡ #{book.favorites.count}", book_favorite_path(book), method: :post %>
<% end %>